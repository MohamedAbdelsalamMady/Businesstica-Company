<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_attendance_list_inherit_device" model="ir.ui.view">
        <field name="name">hr.attendance.list.inherit.device</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.view_attendance_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='check_out']" position="after">
                <field name="is_from_device" invisible="1"/>
                <field name="device_id" optional="show"/>
            </xpath>
        </field>
    </record>

    <record id="view_attendance_form_inherit_device" model="ir.ui.view">
        <field name="name">hr.attendance.form.inherit.device</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.hr_attendance_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='check_out']" position="after">
                <field name="device_id" readonly="1"/>
                <field name="device_timestamp" readonly="1"/>
                <field name="is_from_device" readonly="1"/>
            </xpath>
        </field>
    </record>
</odoo>